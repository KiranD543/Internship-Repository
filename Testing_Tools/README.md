In this directory, different maps can be found for each tool that was tested. Inside these maps, the used input and acquired output can be found. 
Inside the present directory, a commands.txt file can also be found. In this file, all the used commands to execute the pangenome analyses can be found, together with the complementary tool used. 

